<resources>


    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base"/>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/colorPrimary</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">@color/colorAccent</item>

        <!-- You can also set colorControlNormal, colorControlActivated
             colorControlHighlight & colorSwitchThumbNormal. -->
    </style>


    <!-- NotificationActivity custom textview -->
    <style name="notificationCustom">
        <item name="android:padding">30dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">15sp</item>
    </style>


    <!-- MainActivity custom listview -->
    <style name="textViewCustom">
        <item name="android:fontFamily">sans-serif-medium</item>
        <!--<item name="android:background">@color/cyan</item>-->
        <item name="android:paddingLeft">20dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- TaskActivity custom buttons -->
    <style name="btnCustom">
        <item name="android:width">20dp</item>
        <item name="android:textColor">@color/red_A700</item>
    </style>

    <!-- Top Fragment custom dialog, that add tasks -->
    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">#fdf998</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/red_A700</item>
        <item name="android:layout_marginLeft">5sp</item>
        <item name="android:layout_marginRight">5sp</item>
        <item name="android:layout_marginTop">5sp</item>
        <item name="android:layout_marginBottom">5sp</item>


    </style>


</resources>